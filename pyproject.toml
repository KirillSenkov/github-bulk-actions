[project]
name = "github-bulk-actions"
version = "0.1.0"
description = "Actions with my repos batch"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "pydantic-settings>=2.13.1",
]
